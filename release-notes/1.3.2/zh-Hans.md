# 版本 %VERSION%（构建 %BUILD%）

## 修复与优化
- 一个蓝牙设备不会再显示成两行。当系统报告把一个地址列了两次时——刚连接或刚断开的瞬间，或 Mac 上有多个蓝牙控制器——面板会把同一个设备画成两行，两行还共用同一套操作状态。现在每个地址只读取一次。
- 设备电量改为取已连接的那一条。报告里两条都在时，未连接那条是后读的，会盖掉当前电量，行里因此可能显示 macOS 上一次写下的旧读数。
- AirPods 的充电盒改用充电盒图标显示，不再占用「Case」这个词，电量行里最后一个没被翻译的英文词也去掉了。
