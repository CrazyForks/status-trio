# Status popover resource baseline (2026-09)

## Result

No controlled three-run baseline was completed. The user later confirmed Wi-Fi and Bluetooth permissions for the profiling bundle, the development footer, and exactly one running app instance. One fresh-process attempt is excluded because Settings was visibly on screen after the popover close. Later user-driven same-process sessions were exploratory: the user confirmed Settings remained closed after those interactions, but the sessions did not meet the three-fresh-process protocol and their requested markers do not establish exact UI transition times. No controlled CPU hotspot or retained-allocation owner was identified. Do not use these measurements to select Task 2 or claim an optimization.

The acceptance protocol requires three fresh-process sequences with confirmed visibility and stable permissions. One fresh-process attempt was excluded after Settings appeared following close; subsequent sessions reused the same process and are exploratory. No exploratory value is substituted into the controlled acceptance rows below. No CPU or retained-allocation owner verdict is possible.

## Artifact and environment

| Field | Recorded value |
|---|---|
| Repository commit | `c5a624042e55e9eaa446755b000a072363afa0e1` |
| Executable SHA-256 (current measurement artifact) | `32f0f6fc1df2391fe37e7288ad4f8adb8329ede4f751048c582012a047e254dc` |
| Bundle ID | `com.lingsmbp.StatusTrio.dev.popover-resource` |
| Development codename | `Popover Perf` (footer verified by user as `设置… · 开发版 · Popover Perf`) |
| App version / build | `1.3.2 (15)` |
| Build command | `BUNDLE_ID=com.lingsmbp.StatusTrio.dev.popover-resource APP_NAME='Status Trio Popover Perf' DEVELOPMENT_CODENAME='Popover Perf' bash scripts/build-app.sh release no-open` |
| Build SDK | `LC_BUILD_VERSION sdk 26.0` (build script passed its SDK floor check) |
| Host SDK / OS | SDK `27.0`; macOS `27.0 (26A428)` |
| Toolchain | Xcode `27.0 (27A266a)`; Swift `6.4`; outside CI Xcode 26.6 / Swift 6.3.3 |
| Architecture | arm64 |
| Power | AC attached; battery 74%, charging |
| Permissions / enabled sections | User confirmed Wi-Fi and Bluetooth permissions; enabled-section set was not independently recorded |
| Raw artifacts | `/tmp/status-trio-popover-baseline/raw/` (outside Git) |

The local SDK reported 27.0, while the produced executable's `LC_BUILD_VERSION` reports 26.0. The binary satisfies the SDK >=26 requirement. The hash above supersedes `da22b611e79fab093269b6db4a28331878af135be9e084ebd149e601ac0ac94a` recorded by the initial blocked attempt. The initial app/PID snapshot is historical and unclassified; it is not a measurement of the current artifact.

## Concurrent app instances

The initial snapshot contained concurrent Status Trio processes: measured PID `85013`, PID `59584` from the memory-footprint worktree, and PID `72962` from `/Applications/Status Trio.app`. They were left untouched. This initial snapshot is superseded and not used for the later PID 91189 measurements. For the later measurements, the user requested and confirmed a sole profiling app instance, PID `91189`, at `/Users/lingsmbp/.codex/worktrees/popover-resource-profile/status-trio/dist/StatusTrio.app/Contents/MacOS/StatusTrio`; the process-specific snapshots below identify that PID and executable. Other machine activity remains a possible system-level confounder.

## Preliminary unclassified snapshot

At `2026-09-25 17:17:20 CST`, the then-running PID 85013 had elapsed time `02:24`, cumulative CPU `0:21.29`, RSS `169648 KB`, physical footprint `68 MB` (vmmap `68.4 MB`), and lifetime peak footprint `327 MB` (vmmap `327.2 MB`). `vmmap` showed Malloc Small `38.8 MB` dirty/resident, CoreAnimation `2.75 MB`, CG Raster Data `2.83 MB`, CG Image `3.47 MB`, and IOSurface `272 KB`. Settings had been displayed during first launch, and its close state was not verifiable through the available UI channel. This initial, superseded binary snapshot is not an idle or post-close result. Raw command output: `/tmp/status-trio-popover-baseline/raw/baseline-ui-unconfirmed-001.txt`. Environment capture: `/tmp/status-trio-popover-baseline/raw/environment.txt`.

These values do not identify a retained application allocation. No Allocations trace, Time Profiler trace, or before/after marker series was collected for this initial snapshot. The one PID's lifetime peak is not an allocation owner finding.

## Acceptance measurement sheet

`Not captured` means no value was recorded for an accepted controlled run. `Unconfirmed` means a required state or value was not independently established for that row. The excluded attempt and exploratory captures are detailed below, not substituted into these rows.

| run_id / marker | commit / executable SHA-256 | bundle ID | OS / SDK | permissions | enabled sections | power / display | PID / confirmed UI state | elapsed | RSS KB | app CPU s | child CPU s | physical footprint MB / peak | vmmap Malloc Small / CoreAnimation / CG Raster / CG Image / IOSurface | profiler launches | Bluetooth completed reads | Wi-Fi completed reads | volume completed reads | first-open / reopen latency |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| baseline-1 / cold launch, 2 min idle | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / cold launch, 2 min idle | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / cold launch, 2 min idle | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / summary open, 60 s | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / summary open, 60 s | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / summary open, 60 s | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / Bluetooth section visible | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / Bluetooth section visible | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / Bluetooth section visible | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / summary closed, 15 s | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / summary closed, 15 s | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / summary closed, 15 s | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / summary closed, 2 min | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / summary closed, 2 min | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / summary closed, 2 min | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / Wi-Fi detail session | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / Wi-Fi detail session | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / Wi-Fi detail session | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / battery detail session | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / battery detail session | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / battery detail session | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / ten summary cycles (10 s open / 10 s closed) | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / ten summary cycles (10 s open / 10 s closed) | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / ten summary cycles (10 s open / 10 s closed) | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-1 / after cycles, closed 2 min | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-2 / after cycles, closed 2 min | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |
| baseline-3 / after cycles, closed 2 min | `c5a6240…` / `32f0f6fc…` | `com.lingsmbp.StatusTrio.dev.popover-resource` | macOS 27.0 / executable SDK 26.0 | Not captured for accepted row | Unconfirmed | AC, charging / display scale not recorded | Not captured / UI unconfirmed | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured | Not captured |

## Excluded and exploratory measurements

The following captures are preserved to document what was measured. None substitutes for the three controlled fresh-process runs in the plan. Values are app-process RSS and physical footprint from `ps`/`footprint`; they are separate measures and must not be added together. Cumulative app CPU is process CPU time, not host CPU attribution.

### Fresh-process attempt 1 — excluded

PID `91189` was verified at each snapshot by its executable path in the profiling worktree. It used bundle ID `com.lingsmbp.StatusTrio.dev.popover-resource`, codename `Popover Perf`, and SHA-256 `32f0f6fc1df2391fe37e7288ad4f8adb8329ede4f751048c582012a047e254dc`. The user confirmed Wi-Fi and Bluetooth permissions and closed Settings before the idle period. At `17:42:43 CST`, the app snapshot was CPU `1.60 s`, RSS `166672 KB`, and physical footprint `74 MB`. At `17:45:00 CST` (137 seconds later), it was CPU `1.79 s` (+`0.19 s`), RSS `166368 KB`, footprint `73 MB`. The popover was confirmed visible at `17:45:52 CST`: CPU `2.94 s`, RSS `168096 KB`, footprint `81 MB`. At `17:47:12 CST` (80 seconds after that marker), CPU was `3.32 s` (+`0.38 s`), RSS `168032 KB`, footprint `81 MB`. The user closed the popover around `17:47:19 CST`; the `17:47:39` sample showed CPU `3.97 s`, RSS `173328 KB`, footprint `80 MB`. The user then confirmed Settings was visibly on screen after close. Exclude this entire attempt because the required closed-state condition failed. These two CPU deltas do not establish an optimization opportunity.

The all-process Time Profiler trace ran `17:45:02.303–17:48:45.798 CST` (223.495 seconds) and included the opening and close. Its TOC contains the app and system processes, including `system_profiler`, but process presence alone does not establish that a process was spawned by this app or what reads completed. The corrected direct-child watcher recorded one `system_profiler` child, PID `92023` with PPID `91189`, at `17:45:35 CST`, before the visible-open marker; it recorded no other child row. A one-second process sample does not prove completion, result, or device-read attribution. No Task 2 completed-read gate is established.

Raw snapshots and watcher: `/tmp/status-trio-popover-baseline/raw/baseline-1-cold-idle-start-20260925T174243.txt`, `/tmp/status-trio-popover-baseline/raw/baseline-1-cold-idle-2m-20260925T174500.txt`, `/tmp/status-trio-popover-baseline/raw/baseline-1-summary-open-immediate-20260925T174552.txt`, `/tmp/status-trio-popover-baseline/raw/baseline-1-summary-open-60s-20260925T174712.txt`, `/tmp/status-trio-popover-baseline/raw/baseline-1-close-immediate-settings-ambiguous-20260925T174739.txt`, and `/tmp/status-trio-popover-baseline/raw/baseline-1-process-watch.tsv`; profiler TOC: `/tmp/status-trio-popover-baseline/raw/time-profiler-baseline-1-toc.xml`.

### Same-process user-driven exploration — excluded from acceptance

PID `91189` and the executable/hash above remained unchanged. The user manually confirmed Settings closed before the first session and confirmed after close that Settings had not appeared. No CUA launch/reopen, UI automation, or accessibility query was used during these user-driven interactions. Timing markers were requested by the parent; they are not independent records of the user's exact clicks.

For the first interaction, the pre-open marker at `17:50:33 CST` recorded CPU `5.38 s`, RSS `170368 KB`, footprint `76 MB`. The requested-open marker at `17:52:07 CST` recorded CPU `5.52 s`, RSS `170384 KB`, footprint `76 MB`; the actual open time is unconfirmed. The user later confirmed close; the capture timestamp was `17:55:05 CST`, with CPU `6.68 s`, RSS `170816 KB`, footprint `77 MB`. The `17:55:38` follow-up recorded CPU `6.74 s`, RSS `170480 KB`, footprint `76 MB`; the `17:56:59` follow-up recorded CPU `6.83 s`, RSS `170464 KB`, footprint `76 MB`. The latter is 114 seconds after the close snapshot, not an exact two-minute marker. The all-process Time Profiler covered `17:51:41.448–17:53:42.204 CST` (120.755 seconds). It overlaps the requested-open marker but not the confirmed close; because the actual opening time is unknown, coverage of the opening cannot be asserted. Its TOC lists the target app and system processes, including `system_profiler`, but no child relation is demonstrated. The corrected direct-child watcher file is header-only.

A later user-driven interaction requested summary, Wi-Fi detail, and battery detail markers at `18:01:15`, `18:01:45`, and `18:02:13 CST`. The user confirmed at `18:02:48 CST` that the popover was closed and Settings had not appeared. At that close marker, CPU was `8.27 s`, RSS `176368 KB`, footprint `80 MB`. The `18:05:35 CST` follow-up (2 minutes 47 seconds later) recorded CPU `8.59 s`, RSS `175968 KB`, footprint `78 MB`; it is not an exact two-minute sample. The direct-child watcher for this session is also header-only. The user interaction is confirmed, but the requested markers do not prove the exact duration spent on each panel.

An Allocations recording was attempted against PID `91189` for five minutes, beginning around `17:59:45 CST`. `xctrace` reported `Failed to attach to target process`; the 22 MB trace bundle and its TOC were preserved, but the TOC contains no usable allocation table or retained-stack evidence. Do not infer an allocation owner from this failed recording. Raw files: `/tmp/status-trio-popover-baseline/raw/singleproc-1-pre-open-settings-closed-20260925T175033.txt`, `/tmp/status-trio-popover-baseline/raw/singleproc-1-requested-open-user-driven-pending-confirmation-20260925T175207.txt`, `/tmp/status-trio-popover-baseline/raw/singleproc-1-close-user-confirmed-now-20260925T175505.txt`, `/tmp/status-trio-popover-baseline/raw/singleproc-1-close-15s-after-confirmation-20260925T175538.txt`, `/tmp/status-trio-popover-baseline/raw/singleproc-1-close-2m-late-user-confirmed-settings-closed-20260925T175659.txt`, `/tmp/status-trio-popover-baseline/raw/singleproc-1-process-watch.tsv`, `/tmp/status-trio-popover-baseline/raw/time-profiler-singleproc-1-open-close-toc.xml`, the four `singleproc-allocations-*.txt` interaction snapshots, `/tmp/status-trio-popover-baseline/raw/singleproc-allocations-process-watch.tsv`, and `/tmp/status-trio-popover-baseline/raw/allocations-singleproc-1-toc.xml`.

The profiler data did not yield a validated, actionable app CPU hotspot. The available CPU deltas are cumulative snapshots during user-driven sessions; they do not isolate app work to a controlled popover interval. Thus no CPU optimization is supported by this evidence. Task 2's Bluetooth reader gate is unmet: there is no verified direct child before the opening plus completed-read count, and the observed `system_profiler` row is insufficient evidence of a completed device read. Task 3's memory-owner gate is unmet because Allocations attach failed and no retained stack or owner was captured.

### Earlier UI-unconfirmed sample

The first CUA attempt and the preliminary PID 85013 snapshot are superseded by the later artifact identity and are not classified as baseline measurements. The app appeared to receive `applicationShouldHandleReopen`, whose no-Dock path opens Settings; `cua.getApp` can trigger that reopen path. This explains a plausible route for Settings to reappear, but it does not establish that a later accessibility query caused it. For future manual trials, use the menu-bar item directly and confirm Settings state with the user; do not use `cua.getApp` or query accessibility after closing the popover.

## Reader, process, and Task 2 verdict

### Amended deterministic test gate (2026-09-25)

At the user's request, the Task 2 runtime measurement gate was amended for this bounded fix: no app restart or controlled runtime benefit claim is required. A deterministic unit test injects a Bluetooth state monitor that synchronously reports an already-powered-on adapter when ordinary authorized popover activation starts it, plus a reader that immediately completes each no-event read with a known device. The test waits until that device result is published before checking the reader count. Before the fix, the ordinary opening produced **two completed reader invocations** (not merely two requests); the already-active Settings-owned monitor path produced one. After the fix, the ordinary opening must produce one, while the Settings-owned path still explicitly refreshes once. This is test evidence about call behavior only: it does not count `system_profiler` launches in a running app, measure CPU, or establish a runtime performance benefit.

A follow-up regression test exercises the Settings Bluetooth section's separate activation path by activating the injected controller directly while the store's persistent Bluetooth setting remains off. Its baseline failure showed zero completed reads on popover opening and showed that close incorrectly deactivated the pre-existing monitor. After capturing `bluetoothDevices.isActive` before optional popover activation, the test passes with one opening read and confirms close leaves that externally activated monitor running. This closes an activation-ownership case absent from the first two tests.

- `system_profiler` child: one pre-open direct child was sampled in excluded fresh-process attempt 1; none appeared in the later one-second watcher files. No completed reads were measured.
- Bluetooth completed reads per ordinary runtime opening: unknown. The deterministic test above establishes two completed injected-reader calls on the pre-fix code and one after the fix; runtime process/read counts remain uncaptured.
- Redundant Wi-Fi or volume reads: unknown; the user exercised Wi-Fi detail in an exploratory session, but invocation counters were not observed and volume detail was not tested.
- Combined app-plus-child CPU over ten cycles: unavailable; no controlled ten-cycle scenario or usable CPU attribution.
- Application-owned allocation group >=2 MB live after close: unknown; the attempted Allocations recording failed to attach and provided no retained-stack evidence.
- Original controlled-runtime Task 2 eligibility: **not established** because the required runtime completed-read count was not captured. The user-approved amended unit-test gate supports the narrow duplicate-read change. CPU benefit remains unmeasured and is not claimed.

## Task 3 close-path and memory decision

**Decision: no justified memory edit.** The owner-ranked finding is unresolved: no application-owned allocation stack or retained object group can be named or assigned live-byte totals. The Allocations recording failed to attach to PID `91189`, so this is an absence of owner evidence, not proof that the app retains no memory. The available physical-footprint samples are exploratory, not three matched fresh-process runs: one user-driven close sample was `80 MB` and its 2m47s follow-up was `78 MB`; an earlier close sample was `77 MB` and its 114s follow-up was `76 MB`. They do not establish a repeatable post-close delta or a median, and there are no corresponding post-close allocation-stack or `Malloc Small`, CoreAnimation, CG Raster, CG Image, or IOSurface deltas to compare. No group meets the plan's `2 MB` live-allocation and `3 MB` physical-footprint evidence gate.

Source inspection confirms `StatusBarController.popoverDidClose` calls `store.setPopoverVisible(false)` and `store.closePopoverDetails()`. If a detail panel was open, it clears `popover.contentViewController` immediately; otherwise it schedules the existing 60-second release, whose task clears the content controller after the delay when the popover remains closed. The 60-second retention/cache policy remains unchanged because no retained owner has been attributed to it.

The previously run focused lifecycle suites passed 17 tests: popover content-retention state, Wi-Fi scan cadence/close behavior, and battery detail-panel close behavior. These cover the release timing policy and detail collectors, but they do not assert that the AppKit hosting controller deallocates in a live app after the delayed release. The failed allocation trace cannot answer that question. No further lifecycle run was needed for this docs-only decision; actual controller deallocation remains unverified.

## Reproduction commands and verification

The executable provenance commands and their output are summarized above. Raw environment and initial snapshot captures are in `/tmp/status-trio-popover-baseline/raw/environment.txt` and `/tmp/status-trio-popover-baseline/raw/baseline-ui-unconfirmed-001.txt`. `scripts/build-app.sh release no-open` completed successfully and the SDK check passed. The initial `swift test` baseline passed 351 Swift Testing tests in 60 suites; focused lifecycle suite verification for this report amendment is recorded in the task report.

## Candidate exploratory run — excluded from acceptance

The user chose to keep the current candidate running and record the available results. The candidate was built from ref `974aa1fec02217482fe5eb42cd1707cfa376cf5b` (`974aa1f`) with executable SHA-256 `b7394a6404df2dc3d4d160b879effdeb384726bc15017770652b107138687708`. Its bundle ID was `com.lingsmbp.StatusTrio.dev.popover-resource`, and its development footer was `Popover Perf`. The user confirmed Wi-Fi and Bluetooth permissions were enabled, the footer was correct, and Settings was closed. PID `21738` was the only Status Trio app process during the captures and ran from the candidate worktree. The user confirmed the popover was closed before the idle sample, then manually opened and closed it for one later exploratory session. No Settings window appeared. No app restart or additional trial followed.

### Closed idle — 122 seconds

From `2026-09-25 14:09:06Z` to `14:11:08Z`, PID `21738` cumulative CPU rose from `1.70 s` to `1.82 s` (`+0.12 s`); RSS rose from `157952 KB` to `158032 KB` (`+80 KB`). `footprint` reported `68 MB` at both ends. `vmmap` physical footprint changed from `68.3M` to `68.4M`; peak remained `410.7M`. No direct child was present in either census. This is one candidate idle sample, not a matched baseline comparison.

### Manual popover session — one 173-second watcher

The watcher ran from `2026-09-25 14:13:01Z` to `14:15:54Z`. Its first app sample recorded CPU `1.93 s` and RSS `157968 KB`; its final sample recorded CPU `2.73 s` and RSS `157568 KB`, a full-watcher delta of `+0.80 s` CPU and `-400 KB` RSS. `footprint` rounded to `68 MB` at both ends; `vmmap` physical footprint changed from `68.3M` to `67.6M`, with peak `410.7M` at both ends. Malloc Small remained `42 MB` at both endpoints.

The user confirmed the popover was closed at `14:14:19Z` and Settings stayed closed. The exact open time is unknown, so the full-watcher CPU delta cannot be attributed to an open or closed interval. One direct-child census at `14:13:03Z` observed PID `22228`, parent `21738`, running `/usr/sbin/system_profiler -json SPBluetoothDataType`; that sample showed cumulative child CPU `0.01 s` and RSS `5248 KB`. The child appeared in only one approximately one-second census. Its completion and relationship to the manually opened popover are unknown, so this is not a completed Bluetooth read count. No other direct-child census row was recorded. The separate broad text matcher also matched the watcher shell's command line; its apparent profiler rows are discarded.

The available post-close captures were taken at `14:15:17Z`, `14:15:33Z`, and `14:17:19Z`, respectively about 58, 74, and 180 seconds after the user's close reply. Their `vmmap` footprints were `67.7M`, `67.6M`, and `67.7M`; peak was `410.7M` in each. The labels in the raw marker log reflect the originally scheduled sequence, not the requested delays from the close reply. The requested exact immediate, +15-second, and +120-second markers were not captured.

Raw candidate captures are `/tmp/status-trio-popover-next-20260925/raw/candidate-singleproc-idle-20260925T2204.log`, `/tmp/status-trio-popover-next-20260925/raw/candidate-popover-session-20260925T2214.log`, and `/tmp/status-trio-popover-next-20260925/raw/candidate-popover-close-markers-20260925T2214.log`. These observations are exploratory only. They do not replace any required three-fresh-process rows, establish an app-plus-child CPU total, identify a retained allocation owner, or support a CPU or memory improvement claim.
